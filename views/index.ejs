<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" type="text/css" href="/css/app.css" />
</head>
<body>
  <div class="tasks">
    <h1>Tasks</h1>
    <input class="tasks__input" placeholder="add some task..."></input>
    <button class="tasks__add-button">Add</button>
    <ul class="tasks__list">
      <% for (var i = 0; i < tasks.length; i++) {%>
        <li class="task" data-id="<%= tasks[i].id %>">
          <input class="task__checkbox" type="checkbox" <% if (tasks[i].isDone) { %>
            <%= 'checked' %>
          <% } %>/>
          <span class="task__text"><%= tasks[i].name %></span>
          <button class="task__delete-button">Delete</button>
        </li>
      <% } %>
    </ul>
  </div>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  <script src="/js/app.js"></script>
</body>
</html>
